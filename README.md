# README

All site changes are to be made through the ```docs``` folder, where using ```mkdocs build``` will update the ```site``` folder. 

use ```mkdocs serve``` to run a local instance of your changes before deploying.